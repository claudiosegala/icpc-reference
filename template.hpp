#ifndef TEMPHPP
#define TEMPHPP




#include <bits/stdc++.h>
using namespace std;

#define FOR(cont,max)        for(int (cont) = 0,       _MAX = (max);(cont) < _MAX; (cont)++)
#define FOR2(cont,start,max) for(int (cont) = (start), _MAX = (max);(cont) < _MAX; (cont)++)
#define LOG(x)               (31 - __builtin_clz(x))
#define DEBUG(x)             cerr << #x << " = " << x << endl;

using ii = pair<int, int>;
#define ff first
#define ss second
#define ll long long
#define oo 1e9
#define EPS 1e-9
#define pb push_back




#endif//TEMPHPP
